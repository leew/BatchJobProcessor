describe('Audit Module', () => {
    describe('Received request ', () => {
        it('calls dynamoDB client with the expected parameters', async () => {

        });

        it('returns the created job object from dynamo', async () => {
            
        });

        it('logs and throws an error when dynamo fails', async () => {
            
        });
    });
    describe('Imported status is update request ', () => {
        it('calls dynamoDB client with the expected parameters', async () => {

        });

        it('returns the updated job object from dynamo', async () => {
            
        });

        it('logs and throws an error when dynamo fails', async () => {
            
        });
    });
});